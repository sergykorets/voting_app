<% election_part.candidate_participations.order(votes: :desc, position: :asc).each_with_index do |candidate_participation, index| %>
	<%= render partial: "results/candidate_participation", locals: { candidate_participation: candidate_participation, voted: (index < election_part.max_votes_for_candidate) } %>
<% end %>